import 'package:flutter/material.dart';

class FindFirends extends StatefulWidget {
  const FindFirends({Key? key}) : super(key: key);

  @override
  _FindFirendsState createState() => _FindFirendsState();
}

class _FindFirendsState extends State<FindFirends> {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(
        
      ),
    );
  }
}
